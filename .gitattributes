# Shell scripts must keep LF line endings or they break inside Linux
# containers when the repo is cloned on Windows (CRLF in shebang line
# triggers `bash\r: command not found`).
*.sh        text eol=lf
*.toml      text eol=lf
Dockerfile* text eol=lf
